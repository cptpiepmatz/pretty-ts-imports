import GroupByFunction from "../GroupByFunction";
import Import from "../../import_management/Import";

const packageState: GroupByFunction = function(imports) {
  const workingArray: Import[][] = [[], []];
  function worker(imported: Import[]): Import[][] {

  }
};

export default packageState;
